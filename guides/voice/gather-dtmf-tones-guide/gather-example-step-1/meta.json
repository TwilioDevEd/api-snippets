{
  "title": "Branch your call logic based on the digits sent by the user",
  "description": "By default, <Gather> will \"loop\" on the current TwiML URL, requesting the same URL every time input is entered.",
  "type": "server"
}