curl -XPOST https://notify.twilio.com/v1/Services/{Service Instance SID}/Bindings \
    -d "Endpoint={endpoint ID}" \
    -d "Identity=bob" \
    -d "BindingType=gcm" \
    -d "Address={address}" \
    -d "Tag=premium" \
    -d "Tag=new user" \
    -u '{Account SID}:{Auth Token}'