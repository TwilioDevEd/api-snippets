curl -XPOST https://chat.twilio.com/v2/Services/{service sid}/Roles/{role sid} \
    -d "Permission=sendMessage" \
    -d "Permission=leaveChannel" \
    -u '{twilio account sid}:{twilio auth token}'
